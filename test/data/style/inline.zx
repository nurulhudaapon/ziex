pub fn Page(allocator: zx.Allocator) zx.Component {
    return (
        <div @allocator={allocator} @style={
            .display(.flex),
            .row_gap(.px(10)),
        }>
             Inline Style
        </div>
    );
}

const zx = @import("zx");
