pub fn Layout(ctx: zx.LayoutContext, children: zx.Component) zx.Component {
    return (
        <div @allocator={ctx.arena}>
            <Sidebar allocator={ctx.arena} categories={&categories} toc={&toc_links} children={children} />
            <script src=`/assets/docs.js?v={zx.info.version}` defer="true"></script>
        </div>
    );
}
const categories = [_]SidebarProps.Category{
    .{
        .title = "Examples",
        .sections = &[_]SidebarProps.Section{
            .{ .title = "Overview", .href = ssg ++ "/examples" },
            .{
                .title = "Control Flow",
                .items = &[_]SidebarProps.Link{
                    .{ .href = ssg ++ "/examples#if-else", .text = "If/Else" },
                    .{ .href = ssg ++ "/examples#switch", .text = "Switch" },
                    .{ .href = ssg ++ "/examples#for-loop", .text = "For Loop" },
                },
            },
            .{
                .title = "Components",
                .items = &[_]SidebarProps.Link{
                    .{ .href = ssg ++ "/examples#components", .text = "Props" },
                    .{ .href = ssg ++ "/examples#dynamic-attributes", .text = "Dynamic Attributes" },
                    .{ .href = ssg ++ "/examples#form-handling", .text = "Form Handling" },
                },
            },
            .{
                .title = "Caching",
                .href = ssg ++ "/examples#caching",
            },
        },
    },
    .{
        .title = "Example Applications",
        .sections = &[_]SidebarProps.Section{
            .{ .title = "Counter", .href = ssg ++ "/examples/wasm/simple" },
            .{ .title = "Streaming", .href = ssr ++ "/examples/streaming" },
            .{ .title = "Todo App", .href = ssg ++ "/examples/wasm" },
            .{ .title = "Async Timer", .href =  ssr ++ "/examples/wasm/async" },
            .{ .title = "React Integration", .href = ssg ++ "/examples/wasm/react" },
            .{ .title = "Hydration", .href = ssg ++ "/examples/wasm/hydration" },
            .{ .title = "Progress Bar", .href = ssr ++ "/examples/wasm/progress" },
            .{ .title = "Realtime Chat", .href = ssr ++ "/examples/realtime" },
            .{ .title = "Auth", .href = ssr ++ "/examples/auth" },
            .{ .title = "Form", .href = ssr ++ "/examples/form" },
            .{ .title = "File Form", .href = ssr ++ "/examples/form/file" },
        },
    },
};

const toc_links = [_]SidebarProps.Link{
    .{ .href = "/examples#if-else", .text = "If/Else" },
    .{ .href = "/examples#switch", .text = "Switch" },
    .{ .href = "/examples#for-loop", .text = "For Loop" },
    .{ .href = "/examples#components", .text = "Props" },
    .{ .href = "/examples#dynamic-attributes", .text = "Dynamic Attributes" },
    .{ .href = "/examples#form-handling", .text = "Form Handling" },
    .{ .href = "/examples#caching", .text = "Caching" },
};

const Sidebar = @import("../components/sidebar.zx").Sidebar;
const SidebarProps = @import("../components/sidebar.zx").SidebarProps;

const ssr = root.configs.ssr_url;
const ssg = root.configs.ssg_url;
const zx = @import("zx");
const root = @import("root");
