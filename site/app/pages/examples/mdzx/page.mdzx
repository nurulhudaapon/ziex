---
const zx = @import("zx");
const QuickExample = @import("../../overview/overview.zx").QuickExample;
const Greeting = @import("./greeting.mdzx").render;

pub const meta = .{
    .title = "MDZX",
    .description = "Markdown + ZX components, full scenario tour",
};

pub const options: zx.PageOptions = .{};

/// Request context for expressions in the markdown body.
var ctx: zx.PageContext = undefined;

pub fn Page(c: zx.PageContext) zx.Component {
    ctx = c;
    return zx.mdzx.page(@This(), c);
}
---

# MDZX

Markdown pages with embedded ZX, and an explicit `PageContext` when you need it.

You are on:

{ctx.request.pathname}

Also see the pure markdown sibling: [Pure Markdown](/examples/markdown).

<QuickExample />

## Component with props

MDZX components declare `Props` (and usually `var props`); `render` is generated for you.

<Greeting name="Ziex" />

## Formatting

Use **bold**, *italic*, ***bold italic***, ~~strike~~, and `inline code`.

Escaped: \*not emphasis\*

[Home](/) · [Examples](/examples) · <https://ziex.dev>

![ZX logo alt](/favicon.ico)

## Lists

- Unordered item one
- Unordered item two
- Unordered item three

1. First ordered
2. Second ordered
3. Third ordered

- [x] Task done
- [ ] Task open

## Quote & break

> MDZX keeps markdown readable and ZX components first-class.
>
> Nested ideas stay in the quote.

***

## Code fences

Multiple languages closing fences must not leak into the code element.

```js
console.log("fenced code without trailing backticks");
const answer = 42;
```

```zig
const std = @import("std");
pub fn main() void {
    std.debug.print("hello from Zig fence\n", .{});
}
```

```ts
type Point = { x: number; y: number };
export const origin: Point = { x: 0, y: 0 };
```

```
plain fence without a language tag
line two
```

## More headings

# H1 Header
## H2 Header
### H3 Header
#### H4 Header
##### H5 Header
###### H6 Header

## Paragraphs & links

A second paragraph after a blank line. Reference-style later if needed.

Go [home](/) again, or open [Examples](/examples "Examples index").
